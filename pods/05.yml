apiVersion: v1
kind: Pod 
metadata:
  name: pod05

spec:
  containers:
    - name : nginx
      image: nginx
      resources:
        requests:
          memory: "200Mi"
          cpu: "100"
        limits:
          cpu: "200m"
          memory: "400Mi"