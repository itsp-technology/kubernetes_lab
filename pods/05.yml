apiVersion: v1
kind: Pod 
metadata:
  name: pod05

spec:
  containers:
    - name : nginx
      image: nginx
      resources:
        requests:
          cpu: "100m"
          memory: "200Mi"
        limits:
          cpu: "200m"
          memory: "400Mi"